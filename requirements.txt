# Requirements file for REMARK compliance
# This file lists minimum version requirements from pyproject.toml
# For fully pinned dependencies, run: uv pip freeze > requirements.txt
# after installing dependencies with: uv sync

# Core dependencies (minimum versions from pyproject.toml)
ipywidgets>=8.1.7
jupyterlab-myst
jupytext>=1.17.2
matplotlib>=3.9.4
mystmd>=1.7.1
numba>=0.59.0
numpy>=2.0.2
scipy>=1.15.3
sympy>=1.12
voila>=0.5.8

# Git dependency (installed via uv from pyproject.toml)
# econ-ark @ git+https://github.com/econ-ark/HARK@master

# Test dependencies
pytest>=6
pytest-cov>=3
